# Increment 219 Fix

Corrects the Flutter test package import to the actual package name declared by
`apps/mobile/pubspec.yaml`: `tradeforge_mobile`.

No production UI or performance calculation logic is changed.
