name: signal_engine
description: Deterministic signal-candidate and lifecycle domain for TradeForge V2.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.9.0

dependencies:
  risk_engine:
    path: ../risk-engine
  strategy_engine:
    path: ../strategy-engine
  technical_analysis:
    path: ../technical-analysis

dev_dependencies:
  test: ^1.25.0
  lints: ^6.0.0
